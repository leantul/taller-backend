# Copy this file to .env for local Docker Compose usage and fill every blank value.
# Never commit the resulting .env file or reuse production credentials locally.
POSTGRES_PASSWORD=
JWT_SECRET=
BOOTSTRAP_ADMIN_USERNAME=
BOOTSTRAP_ADMIN_PASSWORD=
BOOTSTRAP_ADMIN_FULLNAME=Administrador Taller

# Optional local overrides
POSTGRES_DB=tallerdb
POSTGRES_USER=taller_dev
APP_CORS_ALLOWED_ORIGINS=http://localhost:4200
